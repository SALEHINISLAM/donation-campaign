import React from 'react';
import PropTypes from 'prop-types';

const Donation = props => {
    return (
        <div>
            donation
        </div>
    );
};

Donation.propTypes = {
    
};

export default Donation;